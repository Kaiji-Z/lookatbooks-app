# Product

## Register

product

## Users

中国小企业主、兼职记账人员、代账从业者。年龄跨度大，**显著比例偏向中老年或非技术背景**——他们可能熟练使用微信和网银，但不熟悉现代 Web 应用的下拉、抽屉、模态等交互约定。使用场景：每月一次的关账期（月初 1-10 号最密集），在台式机或笔记本上、在浏览器里完成上个月的账。情绪状态多数是「必须做、有点压力、怕出错」——这是法律义务，记错一笔账可能引发税务风险。

他们的核心诉求不是「炫酷」，而是「**别让我显得蠢，别让我犯错**」。每一步都要告诉他们为什么这么做、做完了是什么状态、下一步该干嘛。

## Product Purpose

lookatBooks 是一款面向中国小企业（适用《小企业会计准则》）的本地记账工具。把每月的银行流水 / 数电票 / 工资表，经「AI 起草 → 人审确认 → 登账锁定 → 期末结转 → 出三大报表 → 导入电子税务局报税」的完整闭环跑通。

工具的产品立场有三条硬约束，UI 必须呼应它们：

1. **LLM 不碰数字**——AI 只提议「科目 + 借贷方向」，金额直接取自单据，确定性引擎强制借贷平衡。UI 永远不能暗示「AI 算出了金额」。
2. **人审闭环**——草稿→待审→已审核→已登账，登账后不可改、只能红字冲销。视觉层级必须让「人确认」这一步始终是承载重心。
3. **法律姿态**——工具做「起草」，不碰认证、不模拟申报。财务报表走电子税务局官方【导入】；提交动作必须由实名自然人完成。

成功长什么样：一个不懂会计的小企业主，在没有客服指导的情况下，独立完成一整月的账，登账后账面平衡、报表能直接交给税务局，过程中没有感到被工具羞辱或迷失。

## Brand Personality

**耐心 · 专业 · 在你身边**

- **耐心**：不假定用户懂会计术语。每个动作都解释为什么、做完会怎样。错误信息说人话，指出路而不是只说「错了」。
- **专业**：凡涉及金额、科目、报表，一律严肃——等宽对齐、tabular figures、表格密度纪律。让用户觉得「这账能交给税务局」。

- **在你身边**：流程不冰冷、不卡死、不甩锅。导航永远在、出口永远有、当前在第几步永远清楚。引导语态像一位坐在旁边的老会计，而不是一道考题。

语态两层：**表层友好（导航/引导/按钮/错误文案）+ 数据层严肃（金额/科目/报表/凭证）**。两层按内容切换，不混用。

## Anti-references

明确不要看起来像：

- **90 年代企业 ERP / 金蝶用友老界面**：密集表格 + 灰底 + 蓝标题栏 + 小字号 + 工具栏塞满图标。这是「财务软件祖传审美」，必须主动远离。
- **2024-2026 通用 AI 工具站**：紫蓝粉渐变 hero、磨砂玻璃卡片、emoji 当主图标、千篇一律的 3 卡网格、`bg-clip: text` 渐变文字。这是 v0/Lovable 默认产物味，会让工具显得「不可托付真账」。
- **银行网银的密集列表界面**：打开就是一堆表格、找不到入口、每个动作都要点三层。月度关账期用户已经紧张，不能再让他们迷路。

## Design Principles

1. **两层语态，按内容切换**——表层（导航、引导、按钮、空状态、错误文案）走友好路线，照顾非会计用户；数据层（金额、科目代码、凭证、报表、试算平衡）一律严肃，走等宽、tabular figures、表格密度纪律。两层各自纯粹，不互相稀释。
2. **数据纪律先于装饰**——金额必须等宽、按小数点对齐、tabular figures；表格不滥用斑马纹；状态用「图标 + 文字 + 颜色」三重编码，绝不靠颜色单一表达。一切装饰让位于「这账能不能一眼读对」。
3. **不假定你懂会计**——每个流程节点配一句「为什么这么做 / 做完是什么状态 / 下一步去哪」。术语第一次出现时给白话注释。错误信息指出路，不只是判对错。
4. **AI 起草、人确认**——视觉层级必须让「人确认 / 人审核 / 人提交」始终是页面承载重心。AI 提议的东西可以低视觉权重地存在，但人的批准动作必须显著、可逆路径必须明显、不可逆操作必须二次确认。
5. **流程永远有出口**——导航持久可见，当前在第几步、上一步去哪、下一步去哪永远清楚。月度工作流是线性主路径，但任何节点都能回到首页、切换期间、查看账套。绝不让用户「卡死无出口」。

## Accessibility & Inclusion

- **目标符合 WCAG 2.1 AA**。正文文字对比度 ≥ 4.5:1，大号文字（≥18px 或粗体 ≥14px）≥ 3:1。占位符文字同样 4.5:1，不允许 muted-gray-on-tinted-white 的低对比默认。
- **默认字号底上抬**——正文默认 ≥ 15px（中文 14px 在 1080p 屏幕上偏小），关键状态与金额 ≥ 16px。
- **状态色盲安全**——现有的绿（已登账）/红（作废）/橙（待审）/蓝（已审）四态对红绿色盲不友好。所有状态必须「图标 + 中文文字 + 颜色」三重编码，颜色只是冗余通道，不是唯一信号。
- **尊重 `prefers-reduced-motion`**——月初关账期是高压场景，HTMX 局部刷新、进度条、状态切换必须有 reduced-motion 回退（即时切换或淡入），不允许依赖大幅位移动画。
- **打印友好的财务报表**——资产负债表 / 利润表 / 现金流量表必须能干净打印（去底色、留结构、金额列对齐），让用户能直接交给会计或税务局。

## References

参考产品拿的不是「长得像」，是具体的设计手法：

- **Linear**（issue tracker）：表层友好 / 数据层冷静的「语态切换」范本——导航和引导是软的，一旦进入 issue/数据就是冷静的等宽与表格密度。lookatBooks 的 month.html 工作流 ↔ 凭证/报表数据层，正复刻这套语态分工。
- **Wave / QuickBooks**：记账工具「上传 → 自动处理 → 人审 → 锁定」的不打扰节奏。lookatBooks 的 5 步月度流程与之间构——拿的是流程节奏和「不假定你懂会计」的引导姿态，不是视觉皮肤。

## UI Architecture (打磨基线 — 2026-07-08)

架构已稳定，下阶段为逐页视觉打磨。记录当前设计决策作为基线：

### 导航栏
- 深色底 (slate-800)，pill 式标签页：月度记账 / 资产 / 报表 / 设置
- URL 路径自动检测当前标签，高亮 `bg-white/15`
- 右侧月份切换器（中文格式 `2026年7月`），左侧账套名（小字）
- HTMX 局部刷新，无 SPA 框架，无页面跳转

### 月度向导 (4 步 + 闸门)
1. **上传** — 拖拽区上传流水/发票/工资表
2. **AI 识别** — AI 帮忙分类(不折叠) + 折旧计提(有资产时) + 审核提示 + 全部审核
3. **体检** — 健康检查结果(✓/⚠/✕) → ✕ 挡住登账/结转
4. **拿报表** — 三大报表 + 报税导出 + 反结账(折叠)
- 进度条 step4 未结账时灰色锁定
- 底部导航 step3 无「下一步」(必须完成结转才自动进 step4)
- 路由安全门：`?step=4` + 未结账 → 自动重定向

### 凭证列表 HERO (永驻，每步可见)
- 摘要行：chevron(▶→旋转) + 凭证号 + 摘要 + 借贷科目流(借:XX 贷:XX) + 金额 + 状态徽章
- 展开后：规范记账凭证表格(表头+合计行)
- 按状态显示操作按钮：
  - 草稿/待审：✓确认 / ✗删除 / 手动修改 / 归到(改科目)
  - 已审核：↩撤回审核 / 手动修改
  - 已登账：红字冲销
  - 已作废：(无按钮)
- reclassify 选择后摘要行+表格实时更新(JS)
- 手动建凭证入口：标题旁 `+ 手动建凭证`

### 凭证详情页
- 草稿/待审：可编辑(科目下拉按类别分组 + 金额输入 + 实时借贷校验)
- 已审核/已登账/已作废：只读表格
- 所有操作跳转围绕 month（不跳其他页面）

### 视觉系统
- **墨与印·松赭琥 (Ink · Seal · Pine · Ochre · Amber)** 五色矿物颜料系统
- 色彩策略: **Restrained** — 中性底 + 语义色 + accent ≤10%
- Tailwind CDN + 自定义 shadow-soft / ef-card / ef-card-flat + OKLCH 全链路
- 数据可视化: 三色系(收入/支出/利润), 非彩虹
- 财务报表表格: ef-report-table 系统(detail/main/sub/subtotal/grand 五档行级)
- WCAG AA 对比度(≥4.5:1), 色盲安全(图标+文字+颜色三重编码)
- reduced-motion 支持, 打印友好(@media print 去阴影留结构)
- 金额 tabular-nums 等宽

### 报表页 (分层: 仪表盘 + 法定表格)
- Hero 区间选择器(from/to 下拉, 所有报表 tab 共享)
- 仪表盘: KPI卡 + ApexCharts 趋势图 + 资产/支出构成图 + 健康指标
- 法定报表: 全部官方行次(IS 32行/BS 53行/CF 22行), 行级格式对齐会计惯例
- 报表格式自动检测: 年报/季报/自定义(按 from/to 区间判断)
- 每张表可单独导出 Excel(携带区间参数)

### 金额显示纪律
- **2位小数强制**: Jinja2 `|money` 过滤器(千分位 + `toFixed(2)`)应用于所有金额渲染
- **图表同步**: ApexCharts tooltip / y轴刻度 / dataLabels 全部统一 `minimumFractionDigits: 2`
- **大数缩写**: 图表轴刻度 ≥1万显示为"N万"(减少视觉噪音), tooltip 仍显示完整金额
- **小切片隐藏**: 甜甜圈图 <8% 的扇区不显示 dataLabel(避免重叠), 鼠标悬停仍可查看

### HTMX 交互模式
- **就地刷新**: 凭证操作(批准/作废/重分类)通过 outerHTML swap 就地更新, 无页面跳转
- **OOB 跨区域**: OOB(out-of-band) swap 实现凭证列表 ↔ 审核区域的联动刷新
- **自定义确认**: 所有 `confirm()` 替换为设计系统弹窗(seal色调), 非 native browser dialog
- **模板热重载**: `.html` 编辑即时生效; `.py` 改动需重启服务器

### 月度流程自动化
- **当月锁定**: 只能报上月及之前的账(当月未结束, 单据不完整), 聚焦"报账"而非实时记账
- **批量上传**: 一次拖入全部文件(银行流水+现金日记账+数电票+工资表), 按扩展名自动识别类型
- **现金日记账**: 文件名/表头含"现金"自动识别为 CASH, 规则镜像银行(1002→1001)
- **自动 bookkeep**: 进入页面时规则引擎自动处理 pending(含交叉匹配, 零AI)
- **自动计提折旧/摊销**: 有资产卡片时, 进入页面自动生成折旧凭证(幂等, 无需手动点击)
- **交叉匹配引擎**: 发票价税合计 ↔ 银行/现金流水金额自动配对, 移除重复付款方, 保留发票标paid
- **强规则体检**: 10项自动检查(借贷平衡/科目合规/库存现金为负/pending残留/应收应付方向/同日同额重复/收入为0)
- **空月结转**: 无交易的月份直接显示结转按钮, 支持季报/年报中间的空月
- **未结账月份提醒**: 前序月份未结账时顶部横幅提示, 点击跳转处理

### 主题系统
- **双主题**: 墨与印(暖灰+朱砂) / 青瓷(冷灰+蓝绿), 设置页切换, localStorage持久化
- **CSS色相变量**: 4个 hue 变量驱动全站77处oklch值, 切主题只改4个数字
- **防闪烁**: head内联脚本, 页面加载前设好 data-theme

### 架构统一
- **MCP tools 是唯一写操作入口**: 54 个 tools, 所有业务逻辑集中于此
- **三条路径完全统一**: CLI命令→MCP tools→核心引擎 / Web路由→MCP tools→核心引擎 / MCP直调→MCP tools→核心引擎
- **入口层零业务逻辑**: CLI和Web只做解析参数→调MCP tool→格式化输出


